fn main(): Int {
    foo = 2 + 3 * ((5 - 1) + 1) / 3
    bar: Int = 7
    return <baz a={foo} b={bar} />
}

fn baz(a: Int, b: Int): Int {
    result = if a > b {
        a
    } elsif a < b {
        b
    } else {
        100
    }

    return result
}
